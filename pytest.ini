[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts =
    --strict-markers
    --tb=short
    -ra
markers =
    integration: requires a running stack (database, broker)
    slow: takes more than a few seconds
