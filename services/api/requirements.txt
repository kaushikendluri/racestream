# Pinned to minor versions: reproducible builds matter more than always being
# on the newest patch for a portfolio stack someone else will clone.
fastapi>=0.115,<0.116
uvicorn[standard]>=0.32,<0.33
websockets>=13.1,<14
httpx>=0.27,<0.29
